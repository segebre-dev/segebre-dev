ignorePatterns: ['pages/*'] # remove
structure:
  children:
    - extension: '*'
    - name: 'src'
      children:
        - name: 'components'
        - name: 'utils'
        - name: 'app'
          ruleId: 'appFolder'
rules:
  appFolder:
    children:
      - name: /^(?:page|layout)$/
        extension: '.ts'
      - name: /^_(?:page|layout)$/
        children:
          - name: /^(?!layout$|page$)${{kebab-case}}/
      - name: /^[^_]/
        ruleId: 'appFolder'
